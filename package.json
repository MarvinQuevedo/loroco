{
  "name": "ozone-web-extension",
  "version": "0.0.1",
  "private": true,
  "description": "Loroco — a Chia wallet that lives in your browser. Send and receive XCH, hold tokens and NFTs, trade with offers, and connect to Chia sites with one click.",
  "type": "module",
  "scripts": {
    "wasm:build": "CC_wasm32_unknown_unknown=/usr/local/opt/llvm/bin/clang AR_wasm32_unknown_unknown=/usr/local/opt/llvm/bin/llvm-ar wasm-pack build vendor/sage/crates/sage-wasm --target web --out-dir ../../../../packages/wallet-wasm --release && pnpm wasm:fix-pkg-name",
    "wasm:build:dev": "CC_wasm32_unknown_unknown=/usr/local/opt/llvm/bin/clang AR_wasm32_unknown_unknown=/usr/local/opt/llvm/bin/llvm-ar wasm-pack build vendor/sage/crates/sage-wasm --target web --out-dir ../../../../packages/wallet-wasm --dev && pnpm wasm:fix-pkg-name",
    "wasm:fix-pkg-name": "node -e \"const fs=require('fs');const p='packages/wallet-wasm/package.json';const j=JSON.parse(fs.readFileSync(p,'utf8'));j.name='@ozone/wallet-wasm';j.private=true;fs.writeFileSync(p, JSON.stringify(j,null,2)+'\\n');\"",
    "dev": "pnpm --filter @ozone/extension dev",
    "build": "node scripts/bump-version.mjs && pnpm wasm:build && pnpm --filter @ozone/extension build",
    "build:fast": "node scripts/bump-version.mjs && pnpm --filter @ozone/extension build",
    "chrome": "node scripts/launch-chrome.mjs",
    "firefox": "node scripts/run-firefox.mjs",
    "build:firefox": "node scripts/bump-version.mjs && pnpm --filter @ozone/extension build:firefox",
    "start": "pnpm build:fast && pnpm chrome",
    "start:firefox": "pnpm build:firefox && pnpm firefox",
    "start:full": "pnpm build && pnpm chrome",
    "zip": "pnpm --filter @ozone/extension zip",
    "zip:firefox": "pnpm --filter @ozone/extension zip:firefox",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "playwright": "^1.60.0",
    "selenium-webdriver": "^4.44.0",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=22"
  }
}
